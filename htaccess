# This htaccess file is used to do version and language redirects for the manual.
RewriteBase /manual

# Redirect people from /manual/ to manual.mixxx.org
RewriteEngine on
RewriteRule ^(.*)$ https://manual.mixxx.org/$1 [R,L]
